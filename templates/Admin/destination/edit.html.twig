{% extends 'base.html.twig' %}

{% block title %}
{{ parent() }} Modification destination
{% endblock %}

{% block body %}
    <h1>Modifier une destination</h1>

    {{ include('admin/destination/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-info" href="{{ path('admin_destination_index') }}">Revenir à la liste des destinations</a>

    {{ include('admin/destination/_delete_form.html.twig') }}
{% endblock %}
